name: Minha primeira linha de montagem
on:
  push:
    branches: [main]

jobs:
  build-e-teste:
    runs-on: ubuntu-latest

  steps:
    -name: 1. Pegar o chassi do carro(codigo-fonte)
    uses: actions/chackout@v3

    -name: 2.Etação de Build(motagem)
    run: echo "construindo o projeto...verificando se todas as peças se encaixam!"

    -name: 3.estaçao de teste(cotole de qualidade)
      run:echo"rodando os teste automotizados...verificando se a porta abre e fecha!"
